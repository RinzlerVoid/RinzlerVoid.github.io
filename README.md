# NEKRONEX Audio Layer

NekroNex uses procedural UI audio generated by `js/motion.js` with the Web Audio API.
No MP3 files are required. The interface respects the user's audio preference via local storage.
